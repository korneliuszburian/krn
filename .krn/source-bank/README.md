# KRN Source Bank

This directory is the local, uncommitted cache for external engineering pattern sources.

Use `.krn/source-bank/repos/` for cloned repositories. Do not commit third-party source trees. Commit only curated manifests, memory notes, source IDs, and KRN implications under `docs/`.

