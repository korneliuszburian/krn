# KRN Benchmark Runtime Artifacts

Local benchmark reports are written here by benchmark eval modules.

Runtime artifacts stay local and are ignored by git. Reviewed lessons belong in `docs/memory`, `docs/plans/canonical/SOURCES.md`, or goal files.
