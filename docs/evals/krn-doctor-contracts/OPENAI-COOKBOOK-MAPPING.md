# OpenAI Cookbook Mapping

This eval follows the KRN eval standard with deterministic local checks.

| Case | Source pattern | KRN mapping |
|---|---|---|
| `valid-fixture-parses` | Goals and ExecPlans require artifact-backed acceptance evidence. | The canonical doctor report example must parse through the product contract. |
| `known-bad-fixture-fails` | Repair-loop and eval patterns require plausible bad cases. | Doctor reports missing caveats must fail to avoid overclaiming readiness. |
| `generated-doctor-report-parses` | Agent improvement loops turn runtime traces into eval definitions. | The CLI-generated readiness report becomes an eval input and must parse through the same contract. |

Green output is readiness-report contract evidence only. It is not productivity, dashboard, or MCP proof.
