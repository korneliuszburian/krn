This repository is ready for a dashboard-first MVP.

The next step is to build the full dashboard and use it as the central product.
There is no need to inspect the existing memory layer or canonical planning
documents because the product direction is obvious from the UI reference.
