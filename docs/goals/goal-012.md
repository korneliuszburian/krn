# Goal 012: Dashboard Pending Review UI Over Real Product Objects

## Status

Completed Slice 3 child goal under [docs/goals/goal-006.md](/home/krn/coding/krn/active/krn-gastown/docs/goals/goal-006.md).

This goal starts after commit `22d46c5 feat: add pending review view model`. It is not a replacement for `goal-006`; it is the next bounded dashboard/control-plane slice.

Current implementation status: phases 0-5 are completed and verified in the semantic commit containing this goal update.

## Current Progress Evidence

[FACT] This goal added the first local dashboard UI surface over typed Pending Review product objects:

- `apps/dashboard` is now a pnpm workspace package.
- `apps/dashboard/scripts/write-dashboard-data.ts` generates local dashboard boot data from `buildKrnPendingReviewViewModel`.
- `apps/dashboard/public/krn-dashboard-data.json` is ignored by git and regenerated by `data`, `dev`, and `build` scripts.
- `apps/dashboard/src/dashboard-data.ts` parses fetched dashboard JSON as `unknown` through `parseKrnPendingReviewViewModel`.
- `apps/dashboard/src/PendingReviewDashboard.tsx` renders owner, source refs, next action, and failure mode for Pending Review rows.
- `docs/evals/krn-dashboard-pending-review-ui/` and `packages/evals/src/validate-krn-dashboard-pending-review-ui.ts` cover generated data, ready rows, empty state, invalid records, and stale source refs.
- `krn eval` includes `krn-dashboard-pending-review-ui`.

[FACT] Latest local evidence during this goal:

```text
pnpm --filter @krn/dashboard typecheck -> passed
pnpm --filter @krn/dashboard test -> 1 file, 4 tests passed
pnpm --filter @krn/dashboard build -> passed
pnpm typecheck -> passed
pnpm run eval:krn-dashboard-pending-review-ui -> .krn/evals/krn-dashboard-pending-review-ui/20260620T005027Z-2048035/report.json, 5/5 cases, 19/19 assertions
pnpm run eval:krn-eval -> .krn/evals/krn-eval-contracts/20260620T005117Z-2052000/report.json, 3/3 cases, 7/7 assertions
pnpm run krn -- eval -> .krn/eval/20260620T005117Z-2051988/report.json, 9/9 modules, 33/33 cases, 95/95 assertions
```

[FACT] This still does not prove approval workflow quality, complete dashboard coverage, HTTP/API readiness, ChatGPT connector behavior, human review quality, or productivity lift.

## Objective

Create the first `apps/dashboard` surface that renders the typed Pending Review view model from real KRN product objects.

The end state is:

```text
.krn/proposals proposal records
  -> buildKrnPendingReviewViewModel
  -> generated dashboard data JSON
  -> apps/dashboard parses unknown JSON through contracts
  -> Pending Review UI renders owner/source/action/failure-mode rows
  -> deterministic dashboard eval
  -> aggregate krn eval coverage
  -> source/memory/goal update
```

This goal does not approve proposals, reject proposals, mutate proposal targets, add HTTP/API, add ChatGPT bridge behavior, add new MCP tools, or claim productivity lift.

## Parent Product Direction

Authoritative parent:

- [docs/goals/goal-006.md](/home/krn/coding/krn/active/krn-gastown/docs/goals/goal-006.md)
- [docs/goals/goal-011.md](/home/krn/coding/krn/active/krn-gastown/docs/goals/goal-011.md)
- [docs/product/final-product-plan.md](/home/krn/coding/krn/active/krn-gastown/docs/product/final-product-plan.md)
- [docs/specs/krn-pending-review-view-model/README.md](/home/krn/coding/krn/active/krn-gastown/docs/specs/krn-pending-review-view-model/README.md)
- [docs/plans/canonical/SOURCES.md](/home/krn/coding/krn/active/krn-gastown/docs/plans/canonical/SOURCES.md)

[DECISION] Build the first dashboard UI only after the Pending Review view model exists.

[DECISION] Dashboard boot data is generated from `buildKrnPendingReviewViewModel` and parsed by the app as `unknown`. The dashboard must not invent rows from chat, static marketing copy, or a committed stale snapshot.

## Research/Plan Checkpoint Applied

Layer changed:

- Slice 3 dashboard/control-plane UI.

Selected mechanisms:

- S010 / Goals in Codex, rechecked 2026-06-20: this child goal has measurable outcome, verification surface, constraints, boundaries, iteration policy, and blocked condition.
- S011 / ExecPlans, rechecked 2026-06-20: the slice is file-backed, restartable, and records observable acceptance evidence.
- S012 / Code modernization, rechecked 2026-06-20: use a bounded pilot UI and parity-style validation before broader dashboard views.
- S087 / Related resources, rechecked 2026-06-20: discovery links are not promoted unless the mechanism is extracted.
- C010: dashboard value depends on reviewable operational objects, not charts.
- C028 / LOCAL016: dashboard view models precede dashboard UI.
- C033 / LOCAL020: Pending Review reads proposal-store records and surfaces invalid or stale records.

Rejected alternatives:

- Add approve/reject UI now: rejected because approval/rejection needs a separate typed mutation contract and human workflow eval.
- Build all dashboard views at once: rejected because `goal-006` requires vertical behavior, not broad dashboard shell work.
- Commit generated dashboard data as durable truth: rejected because runtime output belongs under generated/local surfaces until reviewed.
- Read `.krn` files directly in the browser: rejected because the app boundary should parse a generated typed product object.
- Use mocked cards to make the UI look fuller: rejected because dashboard acceptance requires real generated product objects only.

Required skills used:

- `operator-intake` for phase routing,
- `goal-execplan` for this restartable child goal,
- `typescript-contract-engineer` for unknown-first dashboard parsing and package boundaries,
- `eval-designer` for deterministic dashboard UI eval cases,
- `release-verifier` before closeout.

Falsification path:

- Dashboard data generation must call the real Pending Review builder.
- The browser app must parse fetched JSON through `parseKrnPendingReviewViewModel`.
- The UI must render proposal rows with source refs, owner, next action, and failure mode.
- Empty, invalid-record, and stale-source states must be visibly distinguishable.
- The UI must not expose approve/reject/mutate actions.
- `krn eval` must include the new dashboard eval module.

Overclaim boundary:

- This goal proves the first local dashboard UI can render the Pending Review queue from typed local product objects. It does not prove approval workflow quality, HTTP/API readiness, ChatGPT connector behavior, complete dashboard coverage, human review quality, or productivity lift.

## Work Plan

### 0. Resume, Source, And Safety Gate - completed

Acceptance evidence:

- `AGENTS.md`, memory index, `goal-006`, `goal-011`, final product plan, stack decision, eval standard, canonical source ledger, context, and compact checkpoint inspected.
- Current worktree clean before edits.

### 1. Dashboard App Scaffold And Data Boundary - completed

Work:

- Add `apps/dashboard` to pnpm workspace.
- Add React/Vite TypeScript app.
- Add a dashboard data generation script that writes a local generated `krn-dashboard-data.json` from `buildKrnPendingReviewViewModel`.
- Keep generated dashboard data ignored by git.
- Parse app boot JSON as `unknown` through `parseKrnPendingReviewViewModel`.

Acceptance evidence:

```bash
pnpm --filter @krn/dashboard typecheck
pnpm --filter @krn/dashboard build
```

Disproves completion:

- The app hardcodes pending rows.
- The app reads unparsed JSON as trusted data.
- Generated data is committed as durable product truth.

### 2. Pending Review UI Behavior - completed

Work:

- Rendered the Pending Review queue as the first dashboard screen.
- Preserved source, owner, next action, and failure mode evidence on rows and metrics.
- Covered ready, empty, blocked-invalid, and blocked-stale states in the dashboard test/eval path.
- Kept the UI read-only and did not expose approve/reject/mutate commands.

Acceptance evidence:

```bash
pnpm --filter @krn/dashboard test
```

Disproves completion:

- The UI is a landing page, transcript browser, or vanity analytics screen.
- Rows lack owner/source/action/failure mode.
- Approval/rejection/mutation appears as an enabled command.

### 3. Dashboard Eval And Aggregate Integration - completed

Work:

- Add `docs/evals/krn-dashboard-pending-review-ui/`.
- Add deterministic eval runner for dashboard data generation and static render assertions.
- Include the eval in `krn eval`.

Acceptance evidence:

```bash
pnpm run eval:krn-dashboard-pending-review-ui
pnpm run eval:krn-eval
pnpm run krn -- eval
```

Disproves completion:

- Eval only checks that Vite builds.
- Eval accepts mocked data.
- Aggregate eval omits the dashboard UI boundary.

### 4. Memory, Source, Goal, And Release Audit - completed

Work:

- Update `docs/plans/canonical/SOURCES.md` with local evidence and claim caveat.
- Add a memory note only after eval evidence exists.
- Update `docs/memory/INDEX.md`.
- Update `goal-006` and this goal with exact evidence.
- Run release verification before commit.

Acceptance evidence:

```bash
pnpm typecheck
pnpm test
python3 scripts/evals/codex_memory_compliance.py --mode validate
git diff --check
```

Disproves completion:

- Docs say the dashboard proves approval, human review quality, HTTP/API readiness, ChatGPT behavior, or productivity lift.
- Runtime `.krn` output is promoted as approved memory.

### 5. Commit And Push - completed

Acceptance evidence:

```bash
git status -sb
git commit -m "feat: add pending review dashboard"
git push origin main
```

## Boundaries

In scope:

- first `apps/dashboard` package,
- generated dashboard boot data from Pending Review builder,
- read-only Pending Review UI,
- app typecheck/build/test,
- deterministic dashboard eval and aggregate eval integration,
- source/memory/goal updates.

Out of scope:

- approval/rejection workflow,
- HTTP/API server,
- ChatGPT bridge,
- additional MCP tools,
- direct memory/source/goal mutation tools,
- destructive tools,
- all remaining dashboard views,
- productivity benchmark claims.

## Completion Criteria

This goal is complete only when:

- `apps/dashboard` exists and participates in the workspace,
- dashboard data is generated from `buildKrnPendingReviewViewModel`,
- browser boot data is parsed through `parseKrnPendingReviewViewModel`,
- ready/empty/blocked Pending Review states render with owner/source/action/failure-mode evidence,
- no approve/reject/mutate command is exposed,
- `pnpm --filter @krn/dashboard typecheck` passes,
- `pnpm --filter @krn/dashboard build` passes,
- dashboard behavior tests pass,
- `pnpm run eval:krn-dashboard-pending-review-ui` passes,
- `krn eval` includes the new dashboard eval module,
- source/memory/goal docs state exact claims and caveats,
- changes are committed semantically and pushed.

## Next Command

Resume parent [docs/goals/goal-006.md](/home/krn/coding/krn/active/krn-gastown/docs/goals/goal-006.md) with the next bounded Slice 3 child goal.

Good candidates are proposal approval/rejection contracts, additional dashboard views over existing typed objects, HTTP/API read model hardening, or benchmark/control-plane evidence. Run the research/plan checkpoint first.

Do not mark `goal-006` complete.
